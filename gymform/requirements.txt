# Dependencies for the trainer registration form ONLY.
#
# The form shares this repository with the RTU benchmarking tool but none of
# its code, so it does not need that tool's heavy stack (pandas, PyMuPDF,
# scikit-learn, pdfplumber...). Installing just this file keeps the deployed
# service small enough to run comfortably on a free hosting tier — which the
# full stack does not.
#
#     pip install -r gymform/requirements.txt
#     uvicorn gymform.standalone:app --host 0.0.0.0 --port 8000

fastapi>=0.115.0
uvicorn[standard]>=0.30.6
jinja2>=3.1.4
python-multipart>=0.0.9      # parses the multipart form (the ID proof upload)
requests>=2.32.3             # WhatsApp API calls (Twilio / Meta)
qrcode[pil]>=7.4.2           # the "scan to register" QR code

# Email uses the standard library's smtplib — nothing to install.
